import { Chip8 } from "./model/Chip8";

function main() {
  //Setup graphics
  ;
  // Setup input
  ;

  // Init chip8
  const chip8 = new Chip8();
  // load game
  ;

  // Emulation loop
  while(false /* ? */) {
    // Emulator one cycle
    chip8.emulateCycle();

    // If the draw flag is set, update the screen
    ;

    // Store key press state (Press and release)
    ;
  }
}